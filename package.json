{
	"name": "luffa-ts-template",
	"displayName": "TypeScript Template",
	"type": "module",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"build:superbox": "npm run build",
		"compile": "npm run build",
		"compile:superbox": "npm run build",
		"compile:mp": "npm run build",
		"clean": "node scripts/clean-generated.js",
		"rebuild": "npm run clean && npm run build",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {},
	"devDependencies": {
		"@tcsas/superapp-miniprogram-types": "^0.0.1",
		"typescript": "^5.8.2"
	}
}
